#!/usr/bin/env bash
set -euo pipefail

prefix="${SHARED_CDP_BROWSER_SESSION_PREFIX:-agent}"
timestamp="$(date +%Y%m%d%H%M%S)"
random_hex="$(od -An -N4 -tx1 /dev/urandom | tr -d ' \n')"

printf '%s-%s-%s\n' "${prefix}" "${timestamp}" "${random_hex}"
